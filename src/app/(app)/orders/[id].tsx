/**
 * Order Detail Dynamic Route
 *
 * Matches: /(app)/orders/[id]
 * The `id` param is read by OrderDetailScreen via useLocalSearchParams.
 */
import OrderDetailScreen from '@/screens/orders/OrderDetailScreen';
export default OrderDetailScreen;
